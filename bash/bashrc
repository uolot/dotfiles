#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

set -o vi
PS1='[\u@\h \W]\$ '

DOTBASH=$HOME/.dotfiles/bash
source $DOTBASH/exports
source $DOTBASH/aliases
source $DOTBASH/functions
