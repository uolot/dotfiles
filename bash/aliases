# vim: ft=bash

# Shell

alias ..='cd ..'
alias ...='cd ../..'

alias x="exit"

alias ls='ls --color=auto'
alias ll='ls -lav --ignore=..'   # show long listing of all except ".."

mkcd() {
    mkdir -p $* && cd $*
}
