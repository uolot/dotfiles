# vim: ft=bash

# Bash

alias ..='cd ..'
alias ...='cd ../..'
alias ls='ls --color=auto'
alias ll='ls -lav --ignore=..'   # show long listing of all except ".."

mkcd() {
    mkdir -p $* && cd $*
}


# Vim

alias v="vim"
