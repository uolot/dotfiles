#!/usr/bin/env python

import sys
from math import *

KB = 1024
MB = KB**2
GB = KB**2 * KB


def kb(n):
    return n // KB


def mb(n):
    return n // MB


def gb(n):
    return n // GB


if len(sys.argv) < 2:
    while True:
        print(eval(input(">>> ")))
else:
    print(eval(" ".join(sys.argv[1:])))
