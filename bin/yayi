#/usr/bin/env sh
set -eo pipefail

echo -n "Package name to install [default: $1]: "
read pkg_name

if [ "$pkg_name" == "" ]; then
    pkg_name="$1"
fi
echo "Installing $pkg_name"

yay -S "$pkg_name"
echo $(echo "$pkg_name") >> ~/.dotfiles/new_packages.txt
