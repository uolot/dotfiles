#!/usr/bin/env bash

set -ex

SINK=47   # FIXME
SOURCE=48 # FIXME

pactl set-source-port $SOURCE analog-input-headset-mic
pactl set-source-volume $SOURCE 65650 # ~100%
pactl set-source-mute $SOURCE 0
pactl set-sink-mute $SINK 0
